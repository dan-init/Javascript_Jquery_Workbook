var colors = new Array('white', //example of an array constructor using the keyword 'new' followed the the Array();
                       'black',
                       'custom');

var el = document.getElementById('colors');
el.textContent = colors[0]