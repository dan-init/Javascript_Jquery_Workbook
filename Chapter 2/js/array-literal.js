var colors;
colors = ['white', 'black', 'custom' ]; // assigned a list to the variable colors

var el = document.getElementById('colors');
el.textContent = colors[0]; // assigning the element colors to the first value in the list